# Implementation Plan: Correctness Fixes Batch

**Branch**: `001-correctness-fixes` | **Date**: 2026-02-16 | **Spec**: [spec.md](spec.md)
**Input**: Feature specification from `/specs/001-correctness-fixes/spec.md`

## Summary

Batch fix addressing three correctness issues: (1) LaunchTemplate/
LaunchConfiguration incorrectly priced as EC2 instances (#294), (2)
GetActualCost failing for zero-cost resources (#293), and (3)
golangci-lint compliance with 81+ code quality fixes (#289).
Issue #292 (sparse property confidence signal) is deferred
pending rshade/finfocus-spec#381 (metadata map proto field).

## Technical Context

**Language/Version**: Go 1.25+
**Primary Dependencies**: finfocus-spec v0.5.6 (pluginsdk, pbc), zerolog, connectrpc
**Storage**: N/A (in-memory only)
**Testing**: Go testing with `make test`, `make lint` (golangci-lint)
**Target Platform**: Linux server (gRPC plugin binary)
**Project Type**: Single Go module
**Performance Goals**: GetProjectedCost < 100ms, startup < 500ms
**Constraints**: No proto schema changes; backward compatible
**Scale/Scope**: ~15 files modified, ~50 protogetter fixes

## Constitution Check

*GATE: Must pass before Phase 0 research. Re-check after Phase 1 design.*

| Principle | Status | Notes |
|-----------|--------|-------|
| I. Code Quality & Simplicity | PASS | Zero-cost patterns extend existing maps; confidence signal is a string suffix, not a new abstraction |
| II. Testing Discipline | PASS | Unit tests for each fix area; table-driven for zero-cost and confidence |
| III. Protocol & Interface | PASS | No proto changes; uses existing BillingDetail field; gRPC error codes unchanged |
| IV. Performance & Reliability | PASS | Confidence computation is O(1) per request; no new initialization overhead |
| V. Build & Release Quality | PASS | `make lint` zero issues is an explicit goal; `make test` must pass |

**Post-Phase 1 re-check**: PASS. The BillingDetail bracket suffix
approach stays within the existing proto contract. No new abstractions
or patterns violate constitution principles.

## Project Structure

### Documentation (this feature)

```text
specs/001-correctness-fixes/
├── plan.md
├── research.md
├── data-model.md
├── quickstart.md
├── contracts/
│   └── billing-detail-confidence.md
├── checklists/
│   └── requirements.md
└── tasks.md              (generated by /speckit.tasks)
```

### Source Code (repository root)

```text
internal/
├── plugin/
│   ├── constants.go       # ZeroCostServices + ZeroCostPulumiPatterns (modified)
│   ├── validation.go      # ValidateActualCostRequest zero-cost check (modified)
│   ├── actual.go          # GetActualCost zero-cost routing (modified)
│   ├── projected.go       # Protogetter fixes (modified)
│   ├── actual_test.go     # Zero-cost GetActualCost tests (modified)
│   ├── projected_test.go  # Zero-cost projected tests (modified)
│   ├── enrichment.go      # Protogetter fixes (modified)
│   ├── plugin.go          # Protogetter fixes (modified)
│   ├── recommendations.go # errcheck fix for proto.Clone (modified)
│   └── ...                # Other files with protogetter fixes
├── carbon/
│   ├── gpu_specs.go       # errorlint fix (modified)
│   ├── instance_specs.go  # errorlint fix (modified)
│   └── storage_specs.go   # errorlint fix (modified)
tools/
└── generate-carbon-data/
    └── main.go            # errorlint fix (modified)
.golangci.yml              # Committed to version control (already staged)
```

**Structure Decision**: No new packages or files needed. All changes
modify existing files within the established project structure.

## Complexity Tracking

No constitution violations. No complexity justifications needed.
